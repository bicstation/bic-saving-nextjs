// next.config.js

/** @type {import('next').NextConfig} */
const nextConfig = {
  /* config options here */
  // output: 'standalone' を JavaScript オブジェクトとして記述
  output: 'standalone',
};

module.exports = nextConfig;